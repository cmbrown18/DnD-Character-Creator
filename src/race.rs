///Class that represents a race in Dungeon and Dragons

struct Race{
    name: str,
    first_mod_type: str,
    first_mod_number: i8,
    second_mod_type: str,
    second_mod_number: i8
}

///Uses the parser to build the race struct when given the name of the race
fn pick_race(str:race_name) -> Race{
     //This will take the race name and send it into the parser to find and retrieve the data

}

